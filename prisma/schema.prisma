generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}


model Chat {
  id        Int @id @default(autoincrement())
  sessionId String
  message   String
}